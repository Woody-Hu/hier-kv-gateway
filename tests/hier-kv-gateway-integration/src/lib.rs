//! Hier KV Gateway integration test crate.
//!
//! This crate produces no business code; it only serves as the container for the
//! integration tests under the `tests/` directory. Each integration test file invokes
//! real components directly (no mocks, no shortcuts) to verify the data flow and routing
//! logic between the layers of Hier KV Gateway.
